# 更新日志

本文件记录面向用户的重要变化。项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的结构，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。内部重构只有在改变行为、兼容性或发布风险时才记录。

## [Unreleased]

### Added

- 连接面板新增可复现模拟信号实验室：提供十类固定信号、1–16 通道和五档采样率；随机序列使用固定种子，停止
  后重启从样本 0 复现，运行期间配置锁定，Raw Data 按实际通道数输出。
- 处理面板新增缩放、移动平均和 U16 小端解码三种固定预设；整条链原子展开为现有节点，容量不足时保留原图，
  且不增加工作区 schema 或新的运行时节点类型。
- Modbus RTU 新增仅支持 `01/02/03/04` 的有界只读轮询：首轮立即执行，每轮终态后等待固定间隔，保持单事务在途；
  超时、设备异常或协议错误立即停机且不自动重试，配置与最新结果只保留在会话中并复用现有事务历史。
- 桌面端原始捕获与实时数值 CSV 可共用会话级记录目录；取消选择不改变当前状态，自定义目录无效时不会静默回退。
- 发送栏新增工作区级自动校验尾，支持 CRC-16/MODBUS 与 CRC-32 的两种字节序，以及 XOR-8 和 SUM-8；手动与
  周期发送统一按“展开 payload、追加校验、追加行尾”构造完整帧。
- 发送栏新增 UTF-8、GB18030 与 Windows-1252 文本编码；手动发送、周期发送和自动应答统一使用实际线路字节，
  命令历史会保存并恢复当次编码。
- 桌面串口列表会在连接面板打开或窗口回到前台时静默刷新，并显示不含完整序列号的 USB 设备摘要。
- 桌面串口连接后可动态切换 DTR / RTS；硬件流控接管 RTS 时锁定手动操作，成功值沿用到自动重连。
- 桌面串口连接后以独立状态流监视 CTS / DSR / RI / DCD；单路驱动读取失败显示为“不可用”，不会中断连接。
- 工作区新增按需加载的实时通道监视，分组显示基础、派生与扩展通道的最近 256 点统计，并支持不停止采集的
  本地冻结快照。

### Changed

- VUCAP writer 升级为 v3，记录应用/构建标识、脱敏设备摘要和原生 RX 连接代次/序号/流偏移/单调时间，并为
  固定头与 JSON、每个数据/标记条目及完成 footer 写入独立 SHA-256；reader 继续流式读取 v1/v2，v3 位翻转、
  同代次 RX 不连续或代次倒退确定失败，校验块截断只保留此前已验证的完整前缀；串口 RX 缺少流位置、模拟器记录
  携带流位置或生产者来源与文件头不一致时也会在写入和读取边界被拒绝。
- 处理图编辑器改为首次打开时按需加载，避免预设和节点表单增加首屏脚本体积。
- 工作区 JSON 与本地状态升级为 v13；v1-v10 迁移后默认关闭自动校验尾，v1-v11 补充正弦波、3 通道、25 Hz
  模拟器配置，v1-v12 默认使用 UTF-8 发送编码，历史工作区可继续读取。
- 主导航按核心工作流与高级能力重新分组，设置固定在底部，并移除无操作意义的竖排装饰文字，降低首屏视觉噪声。
- 串口设备摘要压缩为端口与设备信息两层；稳定 USB 身份仅在重连或诊断需要时呈现，不再常驻堆叠小字。
- 暂时移除 GitHub Actions 工作流，构建、测试和候选包生成改为维护者本地手动执行；Dependabot 仅保留依赖更新提醒。
- release 打包会先清理当前 target 的旧 bundle，避免不同构建轮次的安装包混入候选目录。
- 设置页“关于”区域现在显示应用版本、Windows 10/11 x64 支持范围和 MIT 许可证，便于核对候选包身份。
- README 与用户手册改用当前三通道实时工作台截图，避免旧界面与候选包行为不一致。

### Fixed

- JustFloat 帧尾查找改为遇到对齐边界后立即返回的线性前向扫描，同时保留载荷伪帧尾过滤和损坏帧重同步；
  高吞吐读取块不再为每一帧重复扫描后续全部数据。
- 普通串口发送现在等待驱动接受完整帧并发布 TX 事件后才报告成功；连续 10 秒无写入进展会终止连接，部分写入
  失败会显示实际 TX 前缀和已写字节数，避免盲目重试整帧；命令历史与周期发送计数只记录完整写入的帧。
- 切换数据源或基础协议时，终端会保留已有记录并插入清晰的会话边界；边界在 TEXT/HEX 模式下都显示说明文字，
  重复选择当前协议不再清空实时通道。
- 串口断开调用失败时会重新读取后端状态；若设备实际已经断开则恢复真实断开态，若仍连接则保留连接并明确提示
  断开失败，避免前后端状态长期不一致。
- Wasm 扩展运行时升级到不受已知 Use-After-Free 漏洞影响的版本，并在新版实例化 API 下继续拒绝 start 函数，
  保持原有执行栈、燃料和内存边界。
- 波特率改为可直接输入且始终可展开常用值的组合框；移除数字步进和滚轮改值，非法输入保持明确错误并阻止连接，
  修正输入或按 Escape 后才恢复操作。
- Windows 支持的最小窗口内，发送栏、周期发送、频谱设置和状态栏不再因侧栏占宽或窗口变矮而相互覆盖；桌面
  侧栏可随时收起，展开周期发送后终端仍会填满可用高度。
- RX、连接状态或 TX 核心事件监听初始化失败时显示明确故障并阻止连接；Modem 状态、文件发送与 Modbus 等
  可选事件或状态不可用时，不再中断核心串口收发链路。
- 首次打开串口失败、关闭自动重连后的连接丢失及缺少唯一 USB 身份的设备断线现在都会写入脱敏诊断；端口枚举
  失败不再被误报为连接故障。
- 未选中可用串口时连接操作保持禁用并提示原因；断开连接、切换工作区或执行周期任务期间，Enter 不再绕过发送
  按钮的禁用状态。
- 连接侧栏的主操作固定在可见底部，长参数表单滚动后仍能连接或断开；串口刷新完成时不会覆盖更新后的数据源、
  连接状态或较新的端口结果。
- 发送完成不会清空用户在发送期间输入的新草稿；HEX 输入严格拒绝半字节和非法分隔内容，清空终端也会同时清除
  尚未结束的文本残行。
- 侧栏表单补齐稳定的标签、名称和控件关联，波特率、模拟器、串口参数、控制线、通道和设置项可被辅助技术
  准确定位。
- 状态栏吞吐改为按单调时间每秒采样累计字节增量，同时显示即时 RX/TX 速率；空闲、统计重置和后台恢复不再
  继续显示误导性的会话累计平均值。
- VUCAP writer 在更新周期进度前刷新文件缓冲，避免界面进度领先于可观察写入，并更早报告刷新失败。
- 3D 姿态视图在显卡上下文丢失时暂停渲染并显示降级状态，浏览器恢复上下文后自动继续，避免休眠、驱动复位或
  远程桌面切换后静默黑屏。
- 主题设置新增系统模式，可随操作系统配色实时切换，同时保留可持久化的固定深色和浅色模式。
- 自动应答规则存在未保存修改时，不再因选择、添加、启停或删除规则而静默丢失当前草稿。
- FireWater 命名通道兼容严格的 `label=value` 遥测行，同时保留纯数值和 `label:value` 输入。
- Raw Data 模拟器现在发送真实二进制字节，而不是复用 FireWater 文本；进入 Raw 时临时切到 HEX，离开后恢复用户
  原先选择的终端显示模式。
- 波形状态明确区分 `IDLE`、`CONNECTING`、`LIVE` 与 `HISTORY`；连接提示与工作区、回放消息隔离，不再用无关
  瞬时消息覆盖当前连接状态。
- 波形新增基础、派生和扩展通道频谱视图，支持显式采样率、256/512/1024 点周期 Hann 窗、主峰与单边幅值读数。
- Windows 覆盖已有捕获导出文件时改用系统原子替换，避免两次改名之间目标路径短暂缺失。
- 终端跨行原生文本选择只包含当前 TEXT/HEX payload，不再混入时间、方向和字节数。
- 终端 ABS/REL/ΔT 时间基准按钮的无障碍名称现在包含可见缩写，语音控制可按屏幕标签定位。
- 命令历史打开后会聚焦最新条目，Escape 可关闭弹层并把焦点还给历史按钮。

### 早期开发基线（2026-08-26，未公开发布）

#### Added

- 终端命令参考弹层新增 ASCII 0..127 快查，支持按字符、缩写、DEC、HEX 和中文名称查询且不触发发送。
- 命令参考弹层新增独立 TEXT/HEX 校验快算，显示 CRC-16/MODBUS、CRC-32、XOR-8 与 SUM-8，且不改写发送状态。
- 命令参考弹层新增独立字节/数值互转，支持明确类型与大小端，严格拒绝未对齐、越界和非有限输入。
- 桌面串口新增原始文件流式发送，支持显式开始、精确进度、取消、生命周期收尾和发送工作流互斥。
- RX 终端新增按读取块/文本行记录，支持 LF/CRLF/CR、跨分包 UTF-8、有界残行和可见边界警示。
- RX 终端新增 UTF-8、GB18030 与 Windows-1252 文本编码，实时和回放共享工作区选择且不改变原始字节。
- 波形新增共享/独立量程切换；独立模式让混合数量级通道分别自适应，并以焦点通道显示唯一纵轴。
- 波形支持为共享纵轴或各独立通道设置严格有限的固定 Y 量程，并可一键恢复自动量程；数据流边界会清除旧范围。
- 波形双游标新增 A/B 闭区间样本数、最小值、最大值、均值、RMS 和峰峰值统计，并沿用被测通道单位。
- 桌面工作台新增可拖拽与键盘操作的波形/终端分栏，以及主视图和终端专注模式；窄屏固定回退为双面板安全布局。
- 实时波形新增基础/派生通道的上升沿与下降沿单次触发，以当前时间窗完成居中捕获；冻结后接收、终端、统计和
  录制继续。
- 处理图新增帧内字节转数值与数值拆字节节点，覆盖八种数值类型、显式大小端和非法输入 gap。
- Tauri 2 串口工作台，支持有界收发队列、文本/HEX、流控、DTR/RTS、自定义波特率和脱敏诊断。
- FireWater、JustFloat 与 Raw Data 增量数据链路，以及有界波形、处理 DAG、双游标和 3D 姿态视图。
- 工作区、VUCAP 录制与回放、seek、CSV/JSONL/二进制导出和实时数值 CSV。
- 命令历史、安全变量、可取消周期发送、强 USB 身份自动重连和跨端口名恢复。
- 手工发送、周期发送、快捷命令和自动应答支持 CR-only 行尾，并在 TEXT/HEX 模式统一追加单字节 `0D`。
- 有界 RX 自动应答，支持有序 TEXT/HEX 字节规则、跨 chunk/重叠/NUL 匹配和安全命令模板响应。
- 工作区级命名快捷命令支持原始 TEXT/HEX 模板与行尾保真、载入、重命名、排序、删除和有界持久化。
- Modbus RTU 构帧器与单事务主站支持常用 `01/02/03/04/05/06/0F/10`、总线静默、超时/取消、异常响应、
  结构化结果和安全 HEX 草稿回填。
- 终端按当前 TEXT/HEX 内容执行大小写不敏感的字面量搜索、命中高亮和全部/RX/TX 方向过滤。
- 终端新增绝对时间、缓存相对时间和筛选后可见记录间隔，保留倒退时间符号并独立记忆本机显示偏好。
- 终端日志可选择导出全部缓存或当前筛选视图，并对 payload 中的分隔符、换行和反斜杠做单行转义。
- 波形缩放与终端手动上滚可挂起自动跟随，并提供不引发布局跳动的显式恢复入口。
- FireWater / JustFloat 有界解析健康度，显示成功、丢弃、重同步、稳定原因和实时/回放独立统计。
- FireWater / JustFloat 基础通道新增工作区级别名、单位和颜色，协议间隔离，并应用到侧栏、波形、触发、测量、
  姿态菜单和处理图输入选择，而不改变 parser 原名与数值日志。
- 协议解析、处理图和饱和数据平面的固定工作量性能预算、可审阅报告与本地回归门禁。
- 工作区、本地状态、VUCAP 与协议 wire ID 的机器可读兼容矩阵、弃用政策和跨层防漂移门禁。
- 面向当前 UI 的用户手册、故障排查矩阵，以及只接受可复核真实设备证据的硬件报告流程。
- 实验性 `.vux` Wasm 实时附加解析器，提供会话级 RX 授权、独立波形通道、确定性 pack/verify 工具、v1 JSON
  Schema 和由真实 Rust 运行时验证的最小示例。
- Vitest、Playwright、Rust 本地构建、无签名安装包、体积预算和统一版本门禁。
- 每目标 CycloneDX 1.6、完整许可证正文指纹、第三方 NOTICE、安装包内嵌资源和 SHA-256 清单。

#### Changed

- 工作区 JSON 与本地状态升级为 v10；v1-v9 无损迁移，历史处理图、接收编码、记录方式和行尾语义保持不变，
  工作区文件上限为 2 MiB。
- 转换节点复用运行时固定字节缓冲，避免在高频 RX 求值中逐帧创建临时对象，并纳入独立性能预算。
- 实时与回放通道按批写入环形缓冲，每个受影响通道只物化一次 2000 点快照，降低高吞吐主线程开销。
- 兼容性清单升级为 schema v2，以结构化记录实验性扩展标识、包格式、schema/API 版本和未来版本拒绝行为。

#### Fixed

- “导出当前”现在包含未保存的工作副本和名称草稿，同时不会覆盖已保存快照或清除未保存状态。
- 正常关闭应用时会先排空记录队列并提交数值 CSV，不再把完整会话遗留为 `.csv.part`。
- 应用进入关闭收尾后会立即解除波形单次触发，迟到数据不会再冻结即将销毁的视图。
- 周期发送现在允许空文本搭配 LF、CR 或 CRLF，不再拒绝实际包含行尾字节的有效任务。

#### Security

- 发布构建把项目目录与 Cargo Home 重映射为稳定虚拟路径，产物收集前还会扫描裸主程序并拒绝本机路径泄露。
- 主 WebView 使用最小 Tauri capability 与严格 CSP，不执行动态第三方代码。
- 串口输入、缓存、录制、回放、导出、处理图和诊断数据均有显式资源边界。
- 自动应答默认关闭且不响应回放；冷却、32 项 FIFO、单批 64 次匹配、会话 1,000 次及失败停机限制回显环路。
- 连接、来源、工作区、回放或运行环境变化时取消待发送命令，避免旧命令落入新的传输上下文。
- 快捷命令只载入草稿，不展开变量或隐式发送；模板不进入串口诊断，但会明确进入工作区持久化和导出。
- Wasm 扩展强制零 import/start、4 MiB memory、fuel/栈/队列/输入输出边界和会话代次隔离；guest 故障只停用
  扩展。扩展仍在主进程内解释执行，包未签名，SHA-256 不证明发布者身份。
