# 安全策略

## 支持范围

Vofa-Ultra 当前处于预发布阶段。`main` / `master` 和最新的 v0.1.x 版本接收安全修复；尚未承诺对更早的
预发布版本提供补丁。

## 私密报告漏洞

请优先使用 GitHub 仓库“Security”页面中的 Private vulnerability reporting / Security Advisory。报告中
请包含：

- 受影响版本或 commit；
- 操作系统和运行方式；
- 最小复现步骤与实际影响；
- 可行的缓解或修复建议；
- 必要的脱敏日志、截图或概念验证。

如果私密报告入口尚未启用，可创建不含漏洞细节的公开 Issue，请维护者提供私密联系方式。不要在公开 Issue、
PR 或讨论区发布可直接利用的细节。

维护者会先确认影响和复现条件，再协调修复、公告和披露时间。请在修复发布前保持细节私密。

## 数据与日志

串口内容可能包含固件密钥、设备标识、网络凭据或业务数据。提交诊断材料前请删除：

- USB / 芯片序列号和稳定设备标识；
- 访问令牌、密码、私钥和签名材料；
- 未公开固件、协议载荷和客户数据；
- 用户名、绝对路径及其他可识别环境信息。

应用内“串口诊断”导出只记录排障所需元数据，包括生成时间、应用版本、稳定错误码、相对时间、计数、代次及
脱敏串口参数；报告省略端口名、USB 序列号、原始错误文本、绝对路径及 RX / TX 数据。提交前仍应检查报告
内容，并只通过受信任渠道发送。

## 安全边界

- 串口输入是不可信数据，解析器必须保持长度和内存上限。
- Tauri capability 默认最小化，新增文件系统、Shell、进程或网络权限需要单独安全审查。
- 主 WebView 禁止 `eval`、`new Function` 和未隔离的第三方脚本。
- PR 构建不得访问代码签名密钥；签名仅能发生在受保护的发布环境。

## 实验性 Wasm 扩展

- `.vux` 是本地未签名代码。SHA-256 只绑定检查和执行的包/模块字节，不证明发布者身份、来源或安全性。
- 扩展获得的是原始实时 RX 副本，其中可能包含密钥、凭据、设备身份和客户数据；只授权已审查来源。
- Wasmi 使用零 import、fuel、内存、栈、输入/输出和队列限制，但在 Tauri 主进程内解释执行，不是独立 OS 进程
  或容器沙箱。解释器和宿主漏洞仍可能影响应用进程。
- `Module::new` 的 eager 编译有 1 MiB 模块上限和单编译许可，但不受 guest 导出调用 fuel 约束，也不能强制中止；
  撤权只会丢弃迟到结果，不会终止已经开始的编译。
- 后端只能验证 `live-rx.read` 能力字符串，不能证明真实用户点击；WebView、应用包和 CSP 属于授权可信计算基。
- manifest 的 `license` 是作者声明，宿主不验证完整 SPDX 合法性。宿主 SBOM、NOTICE 和 provenance 不覆盖用户
  加载的 `.vux` 或其构建依赖。
- 包路径、检查结果、授权、会话、故障和输出均不持久化；协议、来源、连接、工作区、回放和运行环境边界撤权。

完整 ABI 与资源限制见[实验性 Wasm 协议扩展](docs/extensions.md)。扩展逃逸、越权读取、授权绕过、解析器导致的
宿主崩溃或检查后换包问题均按本安全策略私密报告。
