# Vofa-Ultra

面向嵌入式调试与实时数据采集的现代化跨平台串口工作台。

Vofa-Ultra 学习 VOFA+ 与
[vofa-NEXT](https://github.com/Horldsence/vofa-NEXT) 的产品思路，但采用独立实现。项目优先保证串口
生命周期可靠、数据链路可诊断、内存边界明确，并在基础收发稳定之后再引入高级编排能力。

> 当前代码以可运行的 v0.1.0 MVP 为基础，并已开始实现 v0.2 能力，尚未发布稳定安装包。真实串口仍需在
> 具备 Rust 工具链和串口硬件的环境中验证。

![Vofa-Ultra 实时串口工作台](docs/images/workbench.png)

## 当前能力

- 枚举串口，配置波特率、数据位、校验位、停止位、流控、DTR 与 RTS。
- 后台串口线程负责收发；连接生命周期串行化，TX 队列、单次发送和所有 UI 缓存均有硬上限。
- 文本 / HEX 收发、LF / CRLF 行尾、毫秒时间戳、吞吐统计和终端日志导出。
- FireWater 文本帧与 JustFloat 浮点帧增量解析，支持任意分包和跨 chunk 帧尾。
- uPlot 实时波形、最多 16 通道、时间窗切换、通道显隐和独立暂停。
- 内置模拟数据源，无串口硬件也能贯通协议、波形、终端和发送流程。
- 终端虚拟列表、明暗主题，以及桌面与窄屏响应式布局。
- 命名工作区保存、应用、另存、删除与 JSON 导入导出；切换前安全断开，但不会自动连接设备。
- `.vucap` v1 原始会话录制，Rust 独立 writer、有界背压、RX / TX 单调时间和异常文件检测。
- `.vucap` v1 有界流式 1× 回放，支持完整性检查、暂停、继续、停止、重播和未完成文件有效前缀。
- 可取消的串口自动重连：用 VID、PID 与非空 USB 序列号识别原设备，跨端口名恢复且不猜测歧义设备。
- 256 条有界恢复事件与 128 KiB 脱敏诊断导出，不包含端口名、序列号、原始错误或收发载荷。
- Vitest 单元 / 组件测试、Playwright 浏览器验收和跨平台 GitHub Actions 配置。

## 快速开始

### 浏览器预览

浏览器预览只能使用模拟数据源，适合开发界面与协议逻辑。

```bash
pnpm install
pnpm dev
```

打开 <http://127.0.0.1:1420>，点击“启动模拟”即可看到三通道实时数据。

### 桌面应用

需要 Node.js 22、pnpm 11、Rust 1.88.0 或更高版本，以及
[Tauri 2 平台依赖](https://v2.tauri.app/start/prerequisites/)。

```bash
pnpm install
pnpm tauri dev
```

仓库已提交 `src-tauri/Cargo.lock`，桌面构建应使用锁定依赖。正式发布前仍需在目标平台完成真实串口连接、
收发、拔插和重连验证。

## 协议输入

FireWater 每行是一帧，支持纯数值和命名通道：

```text
1.25,-2,3
temperature:23.5,voltage:3.30
```

JustFloat 每帧由若干个小端序 `float32` 组成，以 `00 00 80 7F` 结尾。Raw Data 模式只进入终端，
不生成波形通道。

## 工作区配置

工作区保存数据源、协议、串口参数、终端收发格式、行尾、时间窗、自动滚动和通道显隐。连接状态、实时波形、
终端记录和统计不会进入配置文件。应用工作区会先断开当前数据源、清空运行数据并恢复显示设置，不会自动连接
保存的设备。

导出文件使用带格式标识的 v1 JSON schema，单文件上限为 64 KiB。导入时会严格校验字段和参数范围，同名配置
会创建带编号的新副本，不会覆盖或自动应用现有工作区。浏览器预览可导入串口工作区，但运行时会安全降级到
模拟器；原始串口配置仍保留在命名快照中。

## 会话录制

Tauri 桌面应用连接数据源后，可在“记录”面板开始录制。原生串口 RX 和每次成功写入设备的 TX 字节分片会
直接进入 Rust writer；Tauri 模拟器通过有界 IPC 队列写入同一格式。停止、断开或切换工作区时会先完成文件
收尾，文件默认保存在系统下载目录的 `Vofa-Ultra` 文件夹中。

捕获文件保存起始时间、数据源、协议、串口参数和带相对单调微秒时间的原始记录。正常文件以统计 footer
结束；磁盘错误或队列溢出会中止录制并保留可诊断的未完成文件，不会静默丢弃数据或拖慢串口 worker。
完整 wire contract 见 [VUCAP 捕获文件格式](docs/capture-format.md)。浏览器预览不开放真实文件录制。

## 会话回放

桌面应用可从“记录”面板的“回放”标签打开任意 `.vucap`，也可直接回放最近一次录制。选择文件后，应用会先
完成仍在进行的录制并断开实时数据源；取消文件选择不会改变当前连接、录制或回放会话。回放使用捕获文件中的
协议和历史时间戳，但不会修改或弄脏当前工作区配置。

后端只保留 reader、一个预读 record 和一个待确认批次。每批受 128 KiB、128 条记录和 16 ms 捕获跨度约束，
前端完成同步入库后才 ACK 下一批。正常 footer 会显示完整状态；缺少 footer 或尾部截断时仍可回放已验证的
完整记录前缀，其他格式损坏会被拒绝。当前版本固定为 1×，尚不提供 seek、倍速、游标和标记。

## 串口恢复与诊断

自动重连默认关闭，只在当前应用运行期间由用户显式启用，不写入工作区或持久化存储。一次手动连接成功后，
应用冻结设备的 USB VID、PID 和非空序列号；意外读写故障会先完成正在录制的捕获文件，再按
`0 / 0.5 / 1 / 2 / 4 / 8 / 16 / 30 / 30 / 30` 秒最多尝试十次。设备改用新的端口名仍可恢复；身份字段
缺失或出现多个相同候选时会停止并要求用户选择，不按旧端口名或同型号猜测。

等待、扫描和连接阶段都可立即取消。后端 generation 门禁会丢弃取消后迟到的打开结果，前端 epoch 同时隔离
迟到的端口枚举和连接 Promise。恢复成功会重置协议流式残片与文本解码边界、重新开始传输统计，但保留已有
波形和终端历史。连接面板可导出有界 JSON 诊断；报告只包含稳定错误码、相对时间、计数和脱敏串口参数。

## 质量检查

```bash
pnpm check
pnpm test:e2e
cargo fmt --manifest-path src-tauri/Cargo.toml -- --check
cargo clippy --locked --manifest-path src-tauri/Cargo.toml --all-targets --all-features -- -D warnings
cargo test --locked --manifest-path src-tauri/Cargo.toml
```

`pnpm check` 会依次执行 ESLint、TypeScript 类型检查、Vitest 和生产构建。Playwright 会验证模拟数据链路、
Canvas 有效像素、工作区导入导出、录制与回放入口权限、发送记录、390 px 窄屏无页面级横向溢出，以及短窗口
发送栏可用性；Tauri IPC 模拟还会验证同一 USB 设备从 `COM3` 迁移到 `COM19` 后的自动恢复流程。

## 设计取舍

- [架构说明](docs/architecture.md)：线程模型、状态机、缓存边界与安全边界。
- [竞品与开源调研](docs/competitive-analysis.md)：从 VOFA+ / vofa-NEXT 继承什么、舍弃什么。
- [路线图](docs/roadmap.md)：从稳定串口核心到记录回放、协议扩展和正式发布。
- [贡献指南](CONTRIBUTING.md)：本地开发、分支、提交和 PR 约定。
- [安全策略](SECURITY.md)：漏洞报告方式与日志脱敏要求。

本项目没有复制 vofa-NEXT 源码。参考项目当前许可证与 README 标注存在不一致，任何后续贡献都必须保持
来源清晰，禁止直接移植许可证不兼容的实现。

## 参与贡献

欢迎提交可复现的问题和边界清晰的 PR。串口相关问题请附操作系统、适配器型号、串口参数和脱敏后的日志，
不要公开设备序列号或其他敏感标识。

## 许可证

[MIT](LICENSE)
